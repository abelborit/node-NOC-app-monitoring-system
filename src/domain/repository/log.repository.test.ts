describe("Test in log.repository.ts", () => {
  test("should ", () => {
    /* aquí sería hacer casi lo mismo que se hizo para el log.datasource.test.ts ya que su implementación es muy similar */
  });
});
